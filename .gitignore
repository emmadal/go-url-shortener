# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below if using dep)
vendor/

# Compiled Object files, Static and Dynamic libs (remove the comment below if using go modules)
*.o
*.a
*.so

# Go Modules cache directory
./.cache

# Output directory
./out

# OS-specific files
Thumbs.db
Desktop.ini
.DS_Store
